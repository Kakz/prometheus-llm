{
    "training": {
        "vocab_size": 50257,
        "d_model": 768,
        "n_layer": 12,
        "n_head": 12,
        "d_ff": 3072,
        "batch_size": 32,
        "max_seq_length": 512,
        "learning_rate": 6.25e-5,
        "warmup_steps": 2000,
        "max_steps": 100000,
        "gradient_accumulation_steps": 1,
        "weight_decay": 0.01,
        "dropout": 0.1,
        "attention_dropout": 0.1
    },
    "logging": {
        "log_interval": 100,
        "save_interval": 1000,
        "save_dir": "checkpoints/stage1",
        "log_file": "training_logs/stage1.log"
    },
    "model": {
        "name": "prometheus_stage1",
        "version": "0.1",
        "description": "Initial training stage configuration similar to GPT-1"
    }
}
